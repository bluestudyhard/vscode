#include<stdio.h>   （时间差问题，首次引用if）
int main()
{int hour1=0;int minute1=0;
 int hour2=0;int minute2=0;
 scanf("%d %d",&hour1,&minute1);
 scanf("%d %d",&hour2,&minute2);
 int t1=hour2-hour1;
 int t2=minute2-minute1;	
 
 if(t2<0) {
 	t2=60+t2;
 	t1 --;
 	
 
 }
 
 
 printf("时间差=%d小时%d分",t1,t2);

    
}


ASCII 码问题
#include<stdio.h>
void main(){
char a,b,c,d;
while(scanf(" %c%c%c",&a,&b,&c)!=EOF)
//从键盘中读取字符，每三个一组，到读完位置。
//空格可以避免回车的读入。
{
//开始排序
 if(a>b) d=a,a=b,b=d;  
 if(a>c) d=a,a=c,c=d;
 if(b>c) d=b,b=c,c=d;
 printf("按各字符的ASCII码从小到大的顺序为：%c %c %c\n",a,b,c);

if(year%4==0&&year%100!=0||year%400)（表示闰年）



3
关于while循环里，要求输入